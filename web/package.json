{
  "name": "nyc-world-web",
  "version": "0.2.8",
  "private": true,
  "type": "module",
  "description": "The New York world service, ported to SvelteKit: static client, REST API, and the authoritative WebSocket game loop.",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server.js",
    "test": "PORT=${PORT:-3000} NODE_PATH=./node_modules node ../server/test-protocol.js",
    "test:ui": "node tests/look-stick.test.mjs && node tests/camera-boot.test.mjs && node tests/water-reflection.test.mjs"
  },
  "dependencies": {
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@sveltejs/adapter-node": "^5.5.7",
    "@sveltejs/kit": "^2.70.3",
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "jsdom": "^28.0.0",
    "svelte": "^5.57.0",
    "vite": "^8.2.2"
  },
  "engines": {
    "node": ">=22"
  }
}
